const sqrt = (x) => x * x;

console.log(sqrt(9));

let names = {
    first: "Gareth",
    last: "Richardson"
};

const getFirstName = (firstName) => {
    console.log(firstName);
}

getFirstName(names.first);